## Packages
date-fns | Timestamp formatting for logs
framer-motion | Smooth animations for list updates and dialogs

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  mono: ["var(--font-mono)"],
}
